import React from "react"
import { Game } from "../GameBoard";

interface Props {
  game: Game
}

const GameStats = (props: Props) => {
  const { game } = props;
  
  return (
    
  )
};

export { GameStats };
